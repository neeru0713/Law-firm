import React from "react";
import NavBar from "./NavBar";

const Header = () => {
  return (
    <div className="w-[100%]">
      <NavBar showButton={true} />
    </div>
  );
};

export default Header;
